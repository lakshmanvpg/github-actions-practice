name: Push Event Practice

on:
  push:
    branches:
      - 'feature/*'

jobs:
  push-test:
    runs-on: ubuntu-latest

    steps:
      - name: Show branch
        run: echo "A push to main triggered this workflow."

